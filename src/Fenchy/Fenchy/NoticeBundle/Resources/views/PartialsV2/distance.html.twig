{# empty Twig template #}


{% if distance < 1000 %}
        {% if distance > 0 %}
	{{ distance }}M
        {% else %}
        {{ distance }}KM
        {% endif %}
{% else %}
	{{ distance_km }}KM
{% endif  %}

